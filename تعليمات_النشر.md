# صدقة جارية — تطبيق PWA قابل للتثبيت

تم تحويل الموقع إلى PWA (Progressive Web App). هذا يعني أن أي شخص يفتح رابط الموقع من المتصفح (على أندرويد أو آيفون) هيقدر يثبّته على شاشة الموبايل الرئيسية ويشتغل كأنه تطبيق عادي (أيقونة مستقلة، بدون شريط المتصفح، ويفتح حتى لو الإنترنت ضعيف).

## الملفات المهمة في هذا المجلد
- `index.html` — الموقع نفسه (بعد إضافة دعم PWA)
- `manifest.json` — بيانات التطبيق (الاسم، الألوان، الأيقونات)
- `service-worker.js` — يسمح بالعمل بدون إنترنت (تخزين مؤقت)
- `icon-*.png` و `apple-touch-icon.png` — أيقونات التطبيق بمقاسات مختلفة

⚠️ **الثلاثة دول لازم يكونوا في نفس المجلد على السيرفر بنفس الأسماء** وإلا التثبيت مش هيشتغل.

---

## الخطوة المطلوبة: رفع الملفات على استضافة HTTPS

PWA **لازم** يكون على رابط حقيقي يبدأ بـ `https://` (التثبيت مش بيشتغل لو فتحت الملف مباشرة من جهازك بدون رفعه على الإنترنت). أسهل وأرخص طرق الاستضافة المجانية:

### الخيار الأول: Netlify (الأسهل، بدون أي خبرة تقنية)
1. روح على netlify.com وسجل حساب مجاني
2. اسحب المجلد ده بالكامل (كل الملفات اللي فيه) وأفلته في صفحة "Sites" → "Add new site" → "Deploy manually"
3. هيعطيك رابط فوري زي `your-site.netlify.app`
4. خلاص — افتح الرابط من الموبايل (أندرويد أو آيفون) وهيظهر زرار "تثبيت التطبيق"

### الخيار الثاني: GitHub Pages (مجاني ودائم)
1. اعمل حساب على github.com
2. اعمل Repository جديد وارفع الملفات كلها
3. من Settings → Pages، فعّل GitHub Pages
4. هيعطيك رابط زي `username.github.io/repo-name`

### الخيار الثالث: Vercel
نفس فكرة Netlify تقريباً — اسحب وأفلت المجلد على vercel.com

---

## بعد الرفع: كيف يثبت المستخدم التطبيق؟

### على أندرويد (Chrome)
- هيظهر تلقائياً زرار "تثبيت التطبيق على الجهاز" فوق الصفحة
- أو من خلال قائمة المتصفح (⋮) → "Install app" / "Add to Home screen"

### على آيفون (Safari فقط — لازم Safari بالتحديد)
- اضغط زرار المشاركة (Share) — المربع وفيه سهم لأعلى
- اختار "Add to Home Screen" / "إضافة إلى الشاشة الرئيسية"
- (آبل ما بتدعمش زرار التثبيت التلقائي، فالموقع بيوضح ده برسالة تلقائية)

---

## لو عايز يظهر في Google Play / App Store فعلياً (خطوة لاحقة اختيارية)

بعد رفع PWA على رابط HTTPS، تقدر تحوله لملف تطبيق حقيقي (APK / IPA) بدون كتابة كود باستخدام:

- **PWABuilder.com** (من مايكروسوفت) — تحط رابط موقعك، وهو يولّدلك ملف Android (APK/AAB) جاهز للرفع على Google Play، وملف iOS تقدر ترفعه على App Store (يحتاج حساب Apple Developer مدفوع 99$/سنة + جهاز Mac أو خدمة بناء سحابية)
- **Median.co** (خدمة مدفوعة لكن بسيطة جداً، بتدعم النشر على المتجرين)

ملحوظة: النشر على Google Play يحتاج حساب مطور مرة واحدة (25$ رسوم لمرة واحدة). النشر على App Store يحتاج حساب مطور Apple (99$ سنوياً) + مراجعة تستغرق أيام.
